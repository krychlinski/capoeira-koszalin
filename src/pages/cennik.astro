---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';
import PageHeader from '../components/PageHeader.astro';
import settings from '../data/settings.json';

const wszystkie = (await getCollection('pricing')).sort((a, b) => a.data.order - b.data.order);
const monthly = wszystkie.filter((p) => p.data.category === 'monthly');
const extra = wszystkie.filter((p) => p.data.category === 'extra');
---

<Base title="Cennik" description="Karnety i opłaty w Akademii Capoeira Koszalin. Pierwszy trening jest bezpłatny.">
  <PageHeader
    eyebrow="Opłaty"
    title="Cennik"
    description="Pierwszy trening zapoznawczy jest bezpłatny i do niczego nie zobowiązuje."
  />

  <section class="sekcja sekcja--ciasna">
    <div class="kontener">
      <h2 class="tytul-grupy">Karnety miesięczne</h2>
      <div class="pozycje">
        {monthly.map((p) => (
          <div class="pozycja">
            <div class="pozycja__opis">
              <h3 class="pozycja__nazwa">{p.data.name}</h3>
              {p.data.description && <p class="pozycja__szczegol">{p.data.description}</p>}
            </div>
            <p class="pozycja__cena">{p.data.price}</p>
          </div>
        ))}
      </div>

      <p class="uwaga">
        Wszystkie opłaty miesięczne są płatne z góry, do 10. dnia miesiąca.
      </p>

      <h2 class="tytul-grupy tytul-grupy--drugi">Opłaty dodatkowe</h2>
      <div class="pozycje">
        {extra.map((p) => (
          <div class="pozycja">
            <div class="pozycja__opis">
              <h3 class="pozycja__nazwa">{p.data.name}</h3>
              {p.data.description && <p class="pozycja__szczegol">{p.data.description}</p>}
            </div>
            <p class="pozycja__cena">{p.data.price}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="sekcja sekcja--ciasna zacheta">
    <div class="kontener zacheta__wnetrze">
      <div>
        <p class="nadtytul">Zanim zapłacisz</p>
        <h2>Przyjdź najpierw<br /><span>na darmowy trening</span></h2>
      </div>
      <div class="zacheta__akcje">
        <a href="/zajecia/" class="przycisk">Sprawdź grafik</a>
        {settings.phone && (
          <a href={`tel:+48${settings.phone.replace(/\s/g, '')}`} class="przycisk przycisk--obrys">
            {settings.phone}
          </a>
        )}
      </div>
    </div>
  </section>
</Base>

<style>
  .tytul-grupy { font-size: clamp(1.25rem, 2.5vw, 1.75rem); margin-bottom: 1.75rem; color: var(--tekst-2); }
  .tytul-grupy--drugi { margin-top: 4rem; }

  .pozycje { border-top: 1px solid var(--linia); }
  .pozycja {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.4rem 0;
    border-bottom: 1px solid var(--linia);
  }
  .pozycja__opis { flex: 1; }
  .pozycja__nazwa { font-size: 1.125rem; margin: 0 0 0.25rem; }
  .pozycja__szczegol { color: var(--tekst-3); font-size: 0.9375rem; margin: 0; }
  .pozycja__cena {
    font-family: var(--font-display);
    font-size: clamp(1.375rem, 3vw, 1.875rem);
    font-weight: 600;
    color: var(--akcent);
    white-space: nowrap;
    margin: 0;
    font-variant-numeric: tabular-nums;
  }

  .uwaga {
    margin-top: 1.5rem;
    padding: 1rem 1.25rem;
    border-left: 2px solid var(--akcent);
    background: var(--tlo-2);
    color: var(--tekst-2);
    font-size: 0.9375rem;
  }

  .zacheta__wnetrze {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }
  .zacheta__wnetrze h2 { margin: 0; }
  .zacheta__wnetrze h2 span { color: var(--akcent); }
  .zacheta__akcje { display: flex; flex-wrap: wrap; gap: 0.9rem; }
</style>
